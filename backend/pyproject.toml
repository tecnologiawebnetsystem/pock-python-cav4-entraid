[project]
name = "pock-python-backend"
version = "0.1.0"
description = "POC FastAPI backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "httpx>=0.27",
    "pydantic>=2.0.0",
    "pyjwt[crypto]>=2.8",
    "python-dotenv>=1.0",
    "truststore>=0.10.4",
]
